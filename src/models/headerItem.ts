type Header = {
    title: string;
    description: React.ReactNode;
    direction: string;
    textDirection: string;
    background:string;
};
type Headers = Header[];

export type {Header,Headers};
